# 20260722
## A
标准的 bitset 背包。
## B
标准的 01 背包。
## C
标准的完全背包。
## D
依旧标准的 bitset 背包。
## E
标准的 01 背包但是多一维。
## F
标准的 01 背包，如果左边部分的奇数等于偶数的话就可以分割，代价为 $|x-y|$，贡献为 $1$。
## G
不标准的 bitset 背包。\
bitset 分别维护能到的点的 $x,y$，因为是独立的。记得加偏移量。
## H
标准的多重背包。
## I
恶心的 01 背包。枚举选不选主件，如果选，怎么选附件。
## J
简单的完全背包。注意到 $\boldsymbol{(m,b)\in(n,a)}$ ，并且 $\forall t\in C_{(n,a)}^{(m,b)},$ 有 $t$ 可以表示为 $(m,b)$ 中钱的组合。然后就可以 `sort` 一下，跑完全背包了，看看能不能省。
## K
难想的 01 背包。$dp_{i,j}$ 表示走到了第 $i$ 棵树，抓了 $j$ 只![](https://espresso.codeforces.com/945648b7e469955ae638081877d7a509b17dd50e.png)剩下的最大魔力。\
转移：枚举 $i$，上一次已经抓了 $j$ 只![](https://espresso.codeforces.com/945648b7e469955ae638081877d7a509b17dd50e.png)，这棵树上要抓 $k$ 只，并且 $dp_{i-1,j}\ge 0,dp_{i-1,j}-k\cdot cost_i\ge 0$ ，此时剩下的魔力为 $\min\{dp_{i-1,j}-k\cdot cost_i+x,w+b(j+k)\}$ 。\
初始化： $dp_{0,0}=w,dp_{i,j}(i+j\neq 0)=-\infty$ 。

## L
？？的？？背包。

AC 之后再补档

---
A-J 板子（？，KL 根本不会啊。
